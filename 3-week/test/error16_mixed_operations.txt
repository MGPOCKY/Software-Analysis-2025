// 타입 오류: 복합적인 타입 불일치
process(ptr, size) {
  var sum;
  sum = ptr;  // 오류: sum에 pointer 할당
  sum = sum + size;  // 오류: pointer + int
  return sum;
}

main() {
  var data, result;
  data = alloc 5;
  result = process(data, 5);
  return result;
}
