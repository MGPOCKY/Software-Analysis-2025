// 타입 오류: 재귀 함수에서 타입 불일치
factorial(n) {
  var result;
  if (n == 0) {
    result = &n;  // 오류: 포인터 할당하지만
  } else {
    result = n * factorial(n - 1);  // 여기서는 int 할당
  }
  return result;
}

main() {
  return factorial(5);
}
